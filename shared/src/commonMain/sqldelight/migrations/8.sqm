ALTER TABLE worry_card ADD COLUMN audio_file_name TEXT;

CREATE TABLE micro_action (
  id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
  title TEXT NOT NULL,
  source_worry_id INTEGER UNIQUE,
  created_at INTEGER NOT NULL
);
