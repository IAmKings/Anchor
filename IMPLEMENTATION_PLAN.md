# Anchor 应用实施计划

> 状态：M0–M6 功能首发已在 Android Debug；当前做 M6 首发页触控/文案对齐。后续顺序见 §8。
> 输入：[PRD.md](PRD.md) · [CONTEXT.md](CONTEXT.md) · [DESIGN.md](DESIGN.md) · [design/README.md](design/README.md)  
> 技术决策：[ADR-0001](docs/adr/0001-local-only-no-account.md) · [ADR-0002](docs/adr/0002-compose-multiplatform.md) · [ADR-0003](docs/adr/0003-anti-kpi-metrics.md)

## 1. 当前基线

- 已建立最小 CMP 工程、Android APK、iOS static framework 与共享检查页；73 项 Stitch 资源保留为实现基准。
- M0 加密存储已接通 SQLDelight 2.3.2 与 Android SQLCipher 4.17.0；生产数据库使用 Android Keystore AES-GCM 封装的随机 256 位口令，真机已通过加密写入、重开读取、错误密钥拒绝、非明文文件头、彻底删除、v1→v11 schema 迁移及密钥复用测试。
- Android 后台计时已采用单调系统时钟；真机通过无权限状态降级及授权后 exact alarm 结束广播测试。ColorOS 对不精确闹钟实测可延迟约 3 天，因此通知准时性需要特殊访问。
- Android 计时结束通知、锁屏脱敏和通知关闭兜底均已通过；四类提醒的 5 个闹钟、上海/伦敦时区自动重排及复评提醒应用内兜底也已通过 Android 16 真机验证。
- Android 生物识别应用锁已通过 Android 16 真机验证：支持生物识别或设备凭据兜底、后台返回重新认证，并在解锁前隐藏应用内容。
- Android 16 真机没有可用的端侧语音识别服务；应用已验证不会调用默认联网识别器，并能自动降级到私有 M4A 录音，退后台自动停止且文件可彻底删除。
- Android 加密导出已通过真机验证：用户密码不落盘，JSON/CSV 仅以 AES-GCM 密文落盘，错误密码拒绝解密，并通过 FileProvider 临时只读 URI 打开系统分享面板。
- Android 多任务隐私遮蔽已通过真机验证：应用锁状态动态控制系统 `FLAG_SECURE`，锁开启时阻止最近任务缩略图、截图和非安全投屏泄露页面内容。
- Android 现有 Compose 首屏 10 次冷启动中位数 418ms、最大 457ms，满足 ≤2 秒红线；页面滚动 306 帧无 deadline missed，危机页延迟仍需在对应实现完成后单独验证。
- 浪潮等待 Compose 原型已在 `PJZ110` 连续运行 15 秒：925 帧、jank 0%、P99 7ms、deadline missed 0；发布前仍需补最低性能档 Android 设备复测。
- M1 安全内核首个切片已完成：纯 Kotlin `SafetyPolicy` 覆盖 PHQ-9/GAD-7 计分分档、第 9 题即时危机触发、关键词澄清、任一量表升档拦截、临床复核动作及间隔 ≥7 天的双低分解除滞后；8 项安全测试通过，Android/JVM 与 iOS Simulator 编译均成功。
- 加密数据库 schema 已升级至 v3，评估逐题答案、得分、分档、动作和 `SafetyState` 在同一事务写入；Android 16 真机通过 v1→当前迁移、关闭重开保持、两次低分恢复、错误密钥拒绝和全部数据清空共 3 项集成测试。
- `AnchorStore` 统一接口与 `InMemoryAnchorStore` 已完成，覆盖评估、安全状态、历史、用户档案和全部清空；内存与 Android SQLCipher 适配通过相同状态演进契约，JVM 与 Android 真机测试均通过。
- 首启年龄自报与 PHQ-9/GAD-7 基线评估 Compose 原型已接入：16 题逐题作答，第 9 题即时危机分流，完成后按安全动作进入正常/就医等待期/危机结果；年龄段与首启完成状态持久化，未完成时启动自动进入评估，完成后重启跳过；Android 真机已确认自动首启入口，青少年/成人分别显示 12355/12356。
- M2 “一次一件”选择与正式首页骨架已接入：正常/轻度用户选择并持久化首个锚点后进入今日首页；浪潮等待保持一键可达；就医等待期首页不显示练习入口，仅呈现专业支持、就医准备与低风险事实记录。
- M3 情绪标签化纵向切片已接入：32 个具体情绪词、结构化事件/难受部分输入、加密卡片保存与回看、标记“已过去”；存储边界拒绝模糊词和空内容，Android SQLCipher 重开与清空测试通过。
- M3 双栏日志纵向切片已接入：摄像头事实/大脑推断双栏、只写一栏降级、评价性事实弱提示但不阻断、保存后的事实反思提示、加密回看；就医等待期仍可使用，Android SQLCipher 重开与清空测试通过。
- M3 浪潮等待完整流程已接入：承认难受、8 个身体定位项与自定义、10 分钟系统时钟倒计时、再加 10 分钟、后台 AlarmManager 到期、锁屏脱敏通知与通知关闭后的应用内横幅；Android 真机后台计时测试通过。
- M4 忧虑保险箱与微行动纵向切片已接入：首页快速挂卡、文字/语音速记、私有 M4A 兜底与专场内播放、20:00–20:20 本地时区专场、三选一和静默顺延；schema v10 在同一事务中把“明天行动”转换为 `MicroAction`，并持久化 1–10 分预测、开始时间、实际体感和完成时间。微行动 5 分钟后台计时复用 AlarmManager，但使用独立偏好、PendingIntent 请求码和通知 ID，不会覆盖浪潮计时；通知不可用时写入应用内兜底。Android 16 真机已验证 v9→v10 迁移、录音播放、测试音频销毁清理、两类计时隔离与 receiver 兜底状态；该 ColorOS 设备未授权精确闹钟时仍可能延迟实际通知。共享状态测试、Android/iOS 编译通过。
- M5 晨间节律首个切片已接入：起床与见光分别一键记录此刻，允许只记一项；见光超过起床一小时只给中性提示，保存后回看最近 7 条且无连续打卡/失败语义。schema v11 加密持久化 `RhythmEntry`，共享验证覆盖空记录拒绝、单项降级、读取和彻底清空；Android 真机已从 v10 正常迁移启动，Android/iOS 编译通过。
- “我的”页已接入二次确认的彻底删除：清空 SQLCipher 记录和本地录音，同时取消提醒、后台计时及通知，删除加密导出缓存并关闭应用锁；成功后回到首启评估，失败时停留并显示原因。
- Android 已接入 `.anchor` 加密备份选择与恢复：解密和完整格式校验成功后，才在单个 SQLCipher 事务中替换本机结构化数据并保留记录 ID/关联；错误密码、损坏文件或不支持版本不会覆盖现有数据。当前导出不包含录音正文，因此恢复时不伪造录音文件。
- M6 无障碍首轮硬化已开始：备份恢复新增覆盖前二次确认；首启量表署名、锚点说明、情绪卡状态及双栏日志弱提示等 12–13sp 小字统一提升至 14sp，并继续保留系统字体缩放能力。
- M6 暗色主题首轮已接入：跟随系统日夜模式切换暖黑背景、深色表面、浅绿主色与浅橙警示色；首发页面的白色卡片、次级文字和状态文字改用 Material 语义色，避免暗色模式下固定浅色表面与低对比文字。
- M6 Android 启动资源已硬化：应用使用自有锚点自适应图标替代系统调试占位图标；系统启动页、状态栏与导航栏按日夜资源匹配 Compose 背景，避免暗色模式冷启动白色闪屏。
- M6 大字号与 TalkBack 首轮真机回归已完成：首启欢迎页支持全页纵向滚动，年龄选项改为全宽纵排；Android 200% 字体下“今天”和“我的/设置”关键操作未截断；TalkBack 已启用，键盘焦点可依次落到浪潮等待与情绪入口等主要操作节点。
- M6 Android Release 构建已通过：补齐 Fragment 1.5.1 依赖后，`lintVitalRelease` 不再阻塞 `assembleRelease`；当前仅保留 Gradle 8.14.2 的升级提示。
- 当前验证顺序已调整：先持续使用 Android Debug 包进行功能测试；功能完善后统一进行最终审计并提交临床/法务复核；iOS 真机验证移至后续版本。Release 签名配置因此不进入当前开发阻塞路径。
- Android Debug 首启安全回归已通过：危机分流、就医等待期、恢复阈值、量表校验和安全状态持久化测试均通过。仪器测试使用隔离库名/prefs/导出目录，不得改写日常 `anchor.db`。
- Android Debug 忧虑闭环回归已通过：忧虑专场 20:00–20:20 边界与下一场时间、忧虑卡转微行动、预测/实际体感记录、提醒时间计算和持久化校验均通过；真机后台通知留待隔离测试数据后验证。
- Android Debug 节律、洞察与导出单元回归已通过。独立数据库的加密存储真机测试已启动并通过前 2/4 用例，但后两例在 ColorOS 设备上长时间无进度，已安全中止；待最终审计阶段单独定位该仪器测试卡顿。
- 地区化危机资源功能已开始：用户档案与 SQLCipher schema v11 增加本地区字段，旧数据默认回退中国大陆；首启选择和危机页资源映射将在下一切片接入，号码有效性仍留待最终审计复核。
- Android 图标已按设计稿校正为暖白底、灰绿色圆线锚点和更宽松留白；自适应、圆形、旧版与启动页图标共用同一矢量，不随暗色模式反转品牌颜色。
- 品牌图标进一步改为 `design/00-brand/anchor-icon.svg` 单一 SVG 母版：使用原稿比例的实心圆环、短粗圆角横梁、圆头立柱和连续弧形锚爪；Android Vector 同步同一组填充路径，不再使用线条近似。
- 根目录 `Anchor-Logo.svg` 已改为透明底、`currentColor` 单色路径，可直接换色和制作启动动效；Android 13+ 自适应图标同时提供 monochrome 图层，支持系统主题图标着色。
- Android 自适应图标、圆形图标、monochrome 主题图标和启动页现已直接复用根目录 `Anchor-Logo.svg` 的两条追踪路径；旧手绘 Logo 路径已删除，SVG 成为唯一视觉源。
- M5 本地洞察首个切片已接入：底部“洞察”页计算最近 30 次起床记录的分钟标准差，以及已完成微行动的平均预测偏差；不足两次起床或没有完整微行动时只显示“记录还不够”。计算适配跨午夜时间并通过 23:50/00:10 边界测试；页面明确数据不上传，不展示 streak、完成率或达标徽章。
- M5 真实数据加密导出已接入“我的”页：用户档案、安全状态、完整量表、情绪卡、双栏日志、忧虑卡、微行动和节律记录输出为 JSON v2 与 CSV，再交给既有 PBKDF2/AES-GCM 容器并通过只读 FileProvider 分享；密码仍不落盘。共享测试覆盖 JSON 控制字符和 CSV 引号转义；录音本体暂不进入归档，仅输出 `hasAudio`，避免导出不可恢复的设备私有路径。Android 真机 instrumentation 本轮停在 ColorOS 测试包安装阶段，生产 APK 构建通过。
- 产品必须纯本地运行：无账号、无后端、无云同步、无埋点、无广告/分析 SDK。
- 首发范围是 PRD §5.1–5.5 与 §5.7；关系耗竭和利他任务属于 P1，不进入首发阻塞路径。
- Compose Multiplatform（CMP）是推荐方向，但 ADR-0002 仍为 `proposed`；通过 M0 原型门后才能改为 `accepted`。
- 危机判定、量表分档和就医等待期状态机是最高优先级安全内核，必须先于普通练习功能完成。
- M6 视觉/触控对齐已完成：轨 A（A1–A10）全部落地。书桌引导、首页声明、仪器测试隔离、录音进导出已补。关系/利他屏 52.dp 与文案抽出已补。洞察能量/利他按次数条与折线已补。下一刀是轨 C 发布阻塞（人与合规）。
- P1 关系耗竭、利他、解释模板、暂时无解计数已进代码，**不挡 Android 首发**；关系/利他屏与洞察次数图已对齐 Stitch 布局（不含百分比）。

## 2. 首版交付边界

### 首发必须包含

1. 首启年龄自报、免责声明、PHQ-9/GAD-7 基线评估。
2. 危机三层判定、危机干预页、地区化热线、就医等待期模式与阈值滞后。
3. 首页、浪潮等待、情绪标签化、双栏日志。
4. 忧虑保险箱、挂卡、忧虑专场。
5. 微行动、预测与实际对比、晨间节律与见光记录。
6. 本地洞察、四类通知白名单、应用锁、加密导出与彻底删除。
7. 无障碍、大字号、锁屏脱敏、多任务缩略图遮蔽。

### 本轮明确不做

- 关系耗竭、关系能量与利他任务（P1）。
- 云同步、账号、社区、排行榜、连续打卡、在线咨询、AI 对话疏导。
- 解释模板聚类、忧虑统计增强等 P1 洞察。
- 自研设计系统框架、通用工作流引擎、依赖注入框架和重型图表库。

## 3. 技术方案与模块

### 工程形态

先采用最小 CMP 工程：

```text
Anchor/
├── shared/          # 共享 Kotlin：业务、数据、Compose UI、测试
├── androidApp/      # Android 启动壳与平台适配
├── iosApp/          # iOS 启动壳与平台适配
└── design/          # 已归档 Stitch 参考资源
```

首版不把每个功能拆成独立 Gradle 模块；先在 `shared` 内按包形成模块，只有构建时间或所有权出现真实问题时才物理拆分。

### 深模块与接口

| 模块 | 小接口提供的能力 | 隐藏的实现复杂度 |
| --- | --- | --- |
| `SafetyPolicy` | 输入量表答案/澄清结果，返回允许状态与下一步动作 | PHQ/GAD 分档、第 9 题即时触发、关键词澄清、阈值滞后 |
| `AnchorStore` | 保存/查询用户记录与状态快照 | SQLDelight、SQLCipher、迁移、事务、彻底删除 |
| `WorrySchedule` | 计算下次专场、判断是否解锁、处理顺延 | 本地时区、22:00 后跨日、时区变化 |
| `ReminderScheduler` | 同步四类允许提醒 | iOS/Android 通知适配、权限关闭后的应用内横幅 |
| `PlatformCapabilities` | 生物识别、语音、系统分享、隐私遮蔽 | iOS/Android 两套原生适配 |
| `LocalInsights` | 从本地记录生成趋势快照 | 预测偏差、30 日稳定度、三个月移动平均 |

`AnchorStore`、`ReminderScheduler` 和 `PlatformCapabilities` 存在生产与测试/双平台适配，因此接口是真实 seam。纯计算逻辑直接使用 Kotlin 类型和函数，不为单一实现额外建立接口或工厂。

### 数据模型首版

- `UserProfile`：年龄档、地区、首启状态、通知偏好。
- `Assessment`：量表类型、逐题答案、分数、分档、完成时间。
- `SafetyState`：正常/就医等待期、触发原因、进入时间、解除评估历史。
- `EmotionCard`：具体情绪词、事件、难受部分、是否已过去。
- `CameraLog`：客观事实、大脑推断、弱提示结果。
- `WorryCard`：内容、封存时间、下次专场、处理结果、关联微行动。
- `MicroAction` / `MicroActionRun`：动作定义、预测分、开始/结束时间、实际分。
- `RhythmEntry`：起床时间、见光时间。
- `ReminderPreference`：四类提醒开关与应用内兜底状态。

除本地数据库主键外，不引入账户 ID、设备 ID 或遥测字段。

## 4. 里程碑与退出条件

### M0 — CMP 平台能力原型门

实现一个不含业务功能的双端原型，验证：

- iOS/Android 启动与共享 Compose 页面。
- SQLCipher 加密写入、读取、迁移与删除。
- 本地通知、时区变化、后台计时结束通知。
- 离线语音识别；失败时保留本地录音。
- 生物识别、系统分享、多任务缩略图遮蔽。
- 浪潮动画在目标低端设备保持可接受流畅度。
- 冷启动 ≤2 秒；危机状态到全屏页渲染 <300ms。

**退出条件**：结果记录进 ADR-0002。全部通过则接受 CMP；关键能力不通过则停止扩建并切换 Flutter，避免沉没成本。

### M1 — 安全内核与加密数据层

- 建立数据 schema、迁移和 `AnchorStore` 两个适配：加密数据库与内存测试适配。
- 完成 PHQ-9/GAD-7 计分、分档、危机三层判定和阈值滞后状态机。
- 内置地区化热线资源与版本字段。
- 完成数据彻底删除和加密导出格式的最小实现。

**退出条件**：安全逻辑分支测试 100%；数据库迁移/加密/删除集成测试通过；所有热线资源可离线加载。

### M2 — 第一条可运行纵向切片

流程：

```text
首启欢迎 → 年龄/地区 → PHQ-9/GAD-7 → 安全判定
  ├─ 正常/轻度 → 一次一件选择 → 首页
  └─ 中重度/高危 → 危机干预 → 就医等待期首页
```

设计基准：`design/01-onboarding-assessment/`、`design/08-medical-safety/`、`design/02-home-daily/`。

**退出条件**：双端真机可完成整条流程；高危路径无法绕过；重启后安全状态保持；屏幕阅读器与大字号完成首轮检查。

### M3 — 当下调节与记录

- 浪潮等待：系统时钟计时、切后台不中断、中性结束。
- 情绪标签化：具体词库、结构化句式、标记已过去。
- 双栏日志：非阻断弱提示、只写一栏降级。
- 首页今日状态与记录入口。

**退出条件**：三条路径均可离线完成并持久化；就医等待期正确屏蔽练习但保留双栏记录。

### M4 — 忧虑保险箱与微行动

- 10 秒挂卡、文字与语音兜底。
- 本地时区下的下一专场计算、开箱、三选一和静默顺延。
- 微行动选择、5 分钟计时、预测/实际记录。
- 忧虑转微行动的跨功能事务。

**退出条件**：跨午夜/跨时区测试通过；语音失败不丢数据；忧虑转微行动具备原子性。

### M5 — 节律、洞察、提醒和数据管理

- 起床/见光记录与 30 日稳定度。
- 预测偏差、三个月移动平均的本地洞察。
- 四类提醒白名单及通知关闭后的应用内横幅。
- 应用锁、加密导出、系统分享、彻底删除。

**退出条件**：无任何第五类通知；无 streak/完成率文案；导出可恢复、删除不可恢复且经二次确认。

### M6 — 发布硬化

- 当前阶段先完成 Android Debug 包的功能验证；最终审计、内容复核、Release 签名和上架合规材料均在功能完善后集中执行。iOS 真机测试不作为本版本退出条件，移至后续版本。
- 视觉对照 73 项资源中的首发相关页面，处理暗色、大字号和低精力状态。
- 性能、崩溃恢复、数据库迁移、备份恢复和隐私检查。
- 临床/法务复核量表译文、关键词样本和医疗宣称。
- 发布前逐条复核热线号码，完成中国区上架合规材料。

**退出条件**：PRD §12.5 质量红线、可行性报告 §5 外部动作全部关闭；发布候选包不包含网络、分析或广告依赖。

## 5. 测试策略

- **纯逻辑测试**：量表、危机判定、阈值滞后、专场调度、指标计算。
- **数据集成测试**：SQLCipher、迁移、事务、导出恢复、彻底删除。
- **平台测试**：通知、语音、生物识别、分享、后台计时和隐私遮蔽。
- **关键 UI 流程测试**：首启安全分流；挂卡到专场；忧虑转微行动；就医等待期屏蔽。
- **视觉检查**：以 `design/manifest.json` 中的 Stitch ID 固定基准，先检查首发页面，不要求实现 P1 设计稿。
- **安全回归样本**：关键词命中、否定表达、引用他人、文学文本、误报澄清和 PHQ-9 第 9 题。

## 6. 外部阻塞项

这些工作可以与开发并行，但未完成不得发布：

1. 临床顾问确认量表题目、危机关键词、误报样本和浪潮曲线参数。
2. 法务确认中文量表署名、隐私政策和非医疗诊断措辞。
3. 产品/运营复核地区热线，补齐澳门处置方案。
4. 技术负责人根据 M0 结果正式接受或拒绝 CMP。

## 7. 第一批开发任务

1. 初始化最小 CMP 双端工程，仅保留 `shared`、`androidApp`、`iosApp`。
2. 建立 M0 原型检查页，不先制作完整导航或业务页面。
3. 为每项平台能力记录真机结果、耗时、失败模式和是否满足门槛。
4. 更新 ADR-0002 状态与证据。
5. 技术栈拍板后立即进入 M1，先写安全内核测试，再写实现。

第一批任务完成前，不引入 SQLDelight/SQLCipher 之外的可选库，也不批量将 73 份 HTML 自动转换为生产 UI；设计稿用于视觉与交互参考，生产 UI 按 Compose 语义和无障碍要求重建。

## 8. 后续实现顺序（2026-08-25）

功能首发（M0–M6）已在代码里。当前工作是 **M6 视觉/触控**：一刀一屏，`heightIn(min = 52.dp)` + 文案进 `*Copy.kt`。CONTEXT / PRD 压过 Stitch。不改 `SafetyPolicy`、schema、热线，除非单独开任务。轻量 Trellis：PRD + jsonl，闸门 `./gradlew :shared:testDebugUnitTest`。

### 轨 A — 首发页触控与文案（按每天会碰到的顺序）

| 序 | 切片 | 状态 | 不要带上 |
| --- | --- | --- | --- |
| A1 | 忧虑三选一 `ProcessStep` | 已完成 | 稿子划掉动效；改成强制三选一 |
| A2 | 忧虑转化 `ConvertStep` | 已完成 | 改 store 事务 |
| A3 | 忧虑完成 `DoneStep` | 已完成 | 加 KPI/完成率 |
| A4 | 微行动挑选 / 预测 / 计时 / 记下 | 已完成 | 个性化推荐稿；完成率 |
| A5 | 微行动历史证据（FR-4.4） | 已完成 | 把洞察图搬过来 |
| A6 | 情绪标签箱 | 已完成 | 改 32 词库 |
| A7 | 记录中心 + 双栏日志 | 已完成 | 评价词改成阻断校验 |
| A8 | 晨间节律 | 已完成 | 补觉/streak 提醒（稿子「设置---晨间节律提醒」与 §6.5 白名单冲突，不用） |
| A9 | 洞察页 | 已完成 | 完成率、诊断口吻 |
| A10 | 设置 / 导出 / 删除 | 已完成 | 录音进备份（另开任务） |

### 轨 B — 首发功能缺口（P0 文案/信息架构）

视觉刀做完对应屏时顺手，或单独开轻量任务：

1. **FR-3.2** 「暂时无解」提示用下次专场动态时间（「[下次专场] 前无需再想」），不写死今晚/明晚 20:00。可附在 A1。
2. **FR-3.2** 开箱缺「在书桌前处理」引导 — 已补（不含必须）。
3. **FR-7.5** 首页声明 — 已改为 PRD 全句。
4. **§11.2** 记录 Tab 增加忧虑保险箱入口 — 已随 A7 落地（等待期仍隐藏）。
5. **仪器测试隔离**：真机用例不得碰到日常库 — 已隔离 DB/锁/计时/导出缓存。
6. **导出不含录音正文** — v4 加密 zip 可带 `audio/*.m4a`；旧备份恢复仍不伪造文件。

不要在 B 里动：量表题目、关键词库、热线号码、滞后解除规则。

### 轨 C — 发布阻塞（人与合规，不是下一刀代码）

与开发并行，未完成不得上架（`docs/FEASIBILITY_CHECK.md` §5）：

1. 按 PRD §10.4 复核热线（澳门仍是 999 + 就近就医）。
2. 临床核量表原文；法务核中文署名。
3. 临床审危机词库和误报样本。
4. 技术负责人根据 M0 证据把 ADR-0002 从 `proposed` 改为接受 CMP（或改 Flutter）。
5. 中国区上架：软著、隐私政策、非医疗诊断宣称。

iOS 真机、Release 签名、临床/法务终审继续后置。

### 轨 D — 明确后置

- 关系/利他 Stitch 对齐 — 已补（52.dp + 文案抽出；未跟开始抽取/防讨好勾选）
- 洞察里关系能量/利他周期稿 — 已补（次数条 + 近 12 条折线；未跟 68%/75%/HRV）
- 录音进入加密导出 — 已随 B6 落地
- iOS 语音/通知/生物识别复测
- 浪潮呼吸曲线临床参数（PRD §9.3 仍开放）
- 行为信号自我觉察（PRD §10.3，P2+）

### 建议排期

```text
现在 → 轨 C 发布阻塞（热线复核、临床/法务、ADR-0002、上架）
     → D 有余力再做
```
